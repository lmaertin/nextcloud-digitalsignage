{
  "name": "digitalsignage",
  "version": "0.8.11",
  "description": "Digital Signage App for Nextcloud",
  "license": "AGPL-3.0",
  "author": "Lukas Märtin",
  "scripts": {
    "build": "echo 'No build step required'",
    "package": "tar -czf digitalsignage-0.8.11.tar.gz --exclude='.git' --exclude='node_modules' --exclude='*.tar.gz' .",
    "test": "phpunit",
    "test:coverage": "phpunit --coverage-html coverage"
  }
}
