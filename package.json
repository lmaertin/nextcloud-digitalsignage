{
  "name": "digitalsignage",
  "version": "0.7.7",
  "description": "Digital Signage App for Nextcloud",
  "license": "AGPL-3.0",
  "author": "Lukas Märtin",
  "scripts": {
    "build": "echo 'No build step required'",
    "package": "tar -czf digitalsignage-0.7.7.tar.gz --exclude='.git' --exclude='node_modules' --exclude='*.tar.gz' .",
    "test": "phpunit",
    "test:coverage": "phpunit --coverage-html coverage"
  }
}
