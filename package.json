{
  "name": "digitalsignage",
  "version": "0.0.5",
  "description": "Digital Signage App for Nextcloud",
  "license": "AGPL-3.0",
  "author": "Lukas Märtin <github@lukas-maertin.de>",
  "scripts": {
    "build": "echo 'No build step required'",
    "package": "tar -czf digitalsignage-0.0.5.tar.gz --exclude='.git' --exclude='node_modules' --exclude='*.tar.gz' .",
    "test": "phpunit",
    "test:coverage": "phpunit --coverage-html coverage"
  }
}
